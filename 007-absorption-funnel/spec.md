# Feature Spec: Absorption Funnel

**Phase**: `007-absorption-funnel`  
**Depends on**: `002` M3–M4 + `003` crawl-log data  
**Status**: Spec scaffold — unclaimed whitespace

Funnel: crawl access → citation → mention → recommendation.

## Acceptance

- Per-URL or per-domain stages with conversion rates
- Drop-off diagnosis (blocked bot vs thin content vs weak entity)
- Uses confidence layer to avoid noise alerts
